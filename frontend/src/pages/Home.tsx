import { Box, Grid } from "@mui/material";

export default function Home() {
    return (
      <Grid
        container spacing={0.5}
        sx={{
          minHeight: "100vh",
          background: "#f2f2f2",
        }}
      >
        
      </Grid>  
    );
}