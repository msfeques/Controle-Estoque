version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: StockControlPostgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Noxus123
      POSTGRES_DB: Stock_DB
    ports:
      - "5432:5432"